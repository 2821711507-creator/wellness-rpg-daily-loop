import { Check, Coins, Sparkles } from 'lucide-react'
import type { Quest } from '../domain/game'
export function QuestBoard({ quests, onComplete }: { quests: Quest[]; onComplete: (id: string) => void }) { return <section className="quest-board"><header><div><p className="eyebrow">오늘의 퀘스트</p><h2>작은 행동, 확실한 보상</h2></div><span>{quests.filter(q => q.completed).length}/{quests.length}</span></header><div className="quest-list">{quests.map(quest => <button className={`quest ${quest.completed ? 'done' : ''}`} key={quest.id} disabled={quest.completed} onClick={() => onComplete(quest.id)}><span className="quest-check">{quest.completed ? <Check /> : null}</span><span><b>{quest.title}</b><small><Sparkles /> {quest.xp} XP <Coins /> {quest.coins}</small></span></button>)}</div></section> }
